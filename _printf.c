#include "main.h"

/**
 * _printf - a set of functions that take a format string as input
 *
 * c - prints a single character
 *
 * Retrun: the number of characters printed
 */
int _printf(const char *format, ...)
{
