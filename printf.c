#include "main.h"

/**
 * _printf - to print formatted text to the standard output stream
 *@format: input strings
 * Return: the number of characters printed
 */

int _printf(const char *format, ...);
{
	
